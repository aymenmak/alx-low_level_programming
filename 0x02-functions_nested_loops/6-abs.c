#include "main.h"

/**
 * _abs - Computers that absolute value of an integer
 * @t: The intiger to be computed
 * Return: The absolute value
 */
int _abs(int t)
{       
        
        if (t >= 0)
                return (t);
        else
                return (-t);
}
